import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';  // Pastikan CSS diimpor dengan benar
import App from './App'; // Pastikan App.jsx diimpor dengan benar

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
